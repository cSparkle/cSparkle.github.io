import * as React from "react"

const ProjectsPage = () => {
    return (
        <main>
            <header>
                <p>Projects</p>
            </header>
      </main>
  )
}

export default ProjectsPage

export const Head = () => <title>Projects</title>
